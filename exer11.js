var altura, base, area, perimetro;

altura = Number(prompt(" Digite o valor de a: "));
base = Number(prompt(" Digite o valor de b: "));

area = (base * altura);
perimetro = ((2 * a) + (2 * b));

alert(area);
alert(perimetro);